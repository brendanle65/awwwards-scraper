{
  "name": "@brendanle/awwwards-scrapers-utils",
  "version": "0.0.0",
  "description": "utility methods for scraping",
  "repository": {
    "type": "git",
    "url": "https://github.com/brendanle65/awwwards-scrapers.git",
    "directory": "packages/utils"
  },
  "author": "brendanle <brendan.mi.le@outlook.com>",
  "license": "UNLICENSED",
  "main": "lib/index.js",
  "@comment scripts": {
    "build": "compile production code",
    "build:watch": "watch and compile code",
    "test": "test code",
    "test:watch": "watch and test code",
    "test:coverage": "collect tests' coverage",
    "clean": "remove compiled code and coverage code"
  },
  "scripts": {
    "build": "rimraf lib/ && tsc",
    "build:watch": "tsc --watch --sourcemap",
    "test": "yarn --cwd ../../ jest --selectProjects utils",
    "test:watch": "yarn --cwd ../../ jest --selectProjects utils --watch",
    "test:coverage": "yarn --cwd ../../ jest --selectProjects utils --coverage",
    "clean": "rimraf lib/ && rimraf coverage/"
  }
}
